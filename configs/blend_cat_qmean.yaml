model:
  name: blend
  params:
    models:
      - name: global_cat
        params: {n_anchors: 56}
      - name: chronos
        params:
          checkpoint: amazon/chronos-bolt-base
          batch_size: 256
          point: qmean
          device: cpu
    weights: [0.5, 0.5]
backtest: {n_origins: 4, horizon: 13, step: 13}
notes: "Cycle 11 Option A (Stage 3 continuation, human-approved beam): straight swap - 50/50 blend of CatBoost-56 (523f9c23da, lumpy bias -1.3%) + Chronos-Bolt qmean (828187da8a, lumpy bias +15.2%). FALSIFICATION ARM: linear bias arithmetic computes lumpy bias +6.9% (CatBoost's neutral lumpy bias deletes the champion's equal-and-opposite cancellation) -> registered prediction: pooled 0.505-0.525 BUT lumpy VN1 >0.712 = computed R4 fail (memo 2026-08-31-cycle11). Weights pinned 50/50. Zero new code. Run with OMP_NUM_THREADS=1 KMP_DUPLICATE_LIB_OK=TRUE. Parent champion 1bb3cb32c6."
