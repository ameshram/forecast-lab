model:
  name: timesfm
  params:
    checkpoint: google/timesfm-2.5-200m-pytorch
    batch_size: 128
    max_context: 512
    max_horizon: 64
backtest:
  n_origins: 4
  horizon: 13
  step: 13
notes: 'Cycle 3B (Stage 1): TimesFM 2.5 200M zero-shot point forecast (memo predicted
  pooled 0.58-0.70; checkpoint swapped from 2.0-500m per memo''s packaging clause
  - installed timesfm 2.0.2 only exposes the 2.5 API)'
