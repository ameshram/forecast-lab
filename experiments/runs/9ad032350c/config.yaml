model:
  name: patchtst
  params:
    freq: W-MON
    accelerator: cpu
    distribution: Poisson
    num_samples: 1000
    model_params:
      input_size: 64
      patch_len: 8
      stride: 8
      max_steps: 1500
      hidden_size: 128
      n_heads: 16
      batch_size: 256
      windows_batch_size: 1024
      learning_rate: 0.001
      scaler_type: robust
      random_seed: 7
backtest:
  n_origins: 4
  horizon: 13
  step: 13
notes: 'Cycle 6B (Stage 2): global PatchTST, Poisson DistributionLoss (retry after
  NB overflow crash in cycle 6, run n/a), no covariates, distribution-mean readout
  (memo 2026-08-29-cycle6b predicted pooled 0.52-0.62, lumpy bias |<10.5%|; parent
  7548b6e864). Poisson verified stable on spiky real subsample incl. identity scaler
  where NB deterministically crashes. lr=1e-3 untuned. Env: neuralforecast==3.2.1,
  torch==2.13.0. Non-bitwise-reproducible (torch); seed=7, num_samples=1000.'
